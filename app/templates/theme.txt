desktop-color: "#000000"
desktop-image: "background.png"
title-text: ""
terminal-font: "$TERMINAL_FONT_NAME $TERMINAL_FONT_SIZE"
terminal-width: "100%"
terminal-height: "100%"
terminal-border: "0"

+ boot_menu {
  width = $MENU_WIDTH
  height = $MENU_HEIGHT
  left = $MENU_LEFT
  top = $MENU_TOP

  item_font = "$ITEM_FONT_NAME $ITEM_FONT_SIZE"
  item_color = "$ITEM_COLOR"
  item_height = $ITEM_HEIGHT
  item_padding = $ITEM_PADDING
  item_spacing = $ITEM_SPACING

  icon_width = $ICON_SIZE
  icon_height = $ICON_SIZE
  item_icon_space = $ITEM_ICON_SPACE

  selected_item_font = "$SELECTED_ITEM_FONT_NAME $SELECTED_ITEM_FONT_SIZE"
  selected_item_color = "$SELECTED_ITEM_COLOR"
  selected_item_pixmap_style = "select_*.png"
}

+ label {
  id = "__timeout__"
  text = "$COUNTDOWN_TEXT"
  width = $COUNTDOWN_WIDTH
  left = $COUNTDOWN_LEFT
  top = $COUNTDOWN_TOP
  align = "$COUNTDOWN_ALIGN"
  color = "$COUNTDOWN_COLOR"
  font = "$COUNTDOWN_FONT_NAME $COUNTDOWN_FONT_SIZE"
}

+ progress_bar {
  id = "__timeout__"
  width = $PROGRESS_BAR_WIDTH
  height = $PROGRESS_BAR_HEIGHT
  left = $PROGRESS_BAR_LEFT
  top = $PROGRESS_BAR_TOP
  align = "$PROGRESS_BAR_ALIGN"
  fg_color = "$PROGRESS_BAR_FOREGROUND_COLOR"
  bg_color = "$PROGRESS_BAR_BACKGROUND_COLOR"
  border_color = "$PROGRESS_BAR_BACKGROUND_COLOR"
}

+ label {
  id = "__timeout__"
  text = "%d"
  width = $PARSED_CIRCULAR_PROGRESS_WIDTH
  left = $PARSED_CIRCULAR_PROGRESS_LEFT
  top = $PARSED_CIRCULAR_PROGRESS_COUNTDOWN_TOP
  align = center
  color = "$CIRCULAR_PROGRESS_COUNTDOWN_COLOR"
  font = "$CIRCULAR_PROGRESS_COUNTDOWN_FONT_NAME $CIRCULAR_PROGRESS_COUNTDOWN_FONT_SIZE"
}

+ image {
  width = $PARSED_CIRCULAR_PROGRESS_IMAGE_WIDTH
  height = $PARSED_CIRCULAR_PROGRESS_IMAGE_WIDTH
  left = $PARSED_CIRCULAR_PROGRESS_IMAGE_LEFT
  top = $PARSED_CIRCULAR_PROGRESS_IMAGE_TOP
  file = "image.png"
}

+ circular_progress {
  id = "__timeout__"
  width = $PARSED_CIRCULAR_PROGRESS_WIDTH
  height = $PARSED_CIRCULAR_PROGRESS_WIDTH
  left = $PARSED_CIRCULAR_PROGRESS_LEFT
  top = $PARSED_CIRCULAR_PROGRESS_TOP
  center_bitmap = "center.png"
  tick_bitmap = "tick.png"
  num_ticks = $CIRCULAR_PROGRESS_NUM_TICKS
  ticks_disappear = $CIRCULAR_PROGRESS_TICKS_DISAPPEAR
  start_angle = $CIRCULAR_PROGRESS_START_ANGLE
}
